name: Build & Deploy Extensions Page

on:
  # 1. Déclenchement manuel
  workflow_dispatch:

  # 2. Modification du script ou template
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - '.github/workflows/**'

  # 3. Réception d'un événement depuis un autre repo (release publiée)
  repository_dispatch:
    types: [release_published]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate index.html
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/generate.js

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages
          exclude_assets: '.github,scripts,node_modules'
s